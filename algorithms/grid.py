def main():
    t_case = input()
    t_case = int(t_case)
    init_pos_li = []
    dim_ar_li = []
    for j in range(t_case):
        dim = input()
        dim = int(dim)
        path = input()
        path = int(path)
        init_pos = input().split()
        dim_ar = input().split()
        init_pos = map[int(init_pos),for l in init_pos]
        dim_ar = map[int(dim_ar),for m in dim_ar]
        init_pos_li.append(init_pos)
        dim_ar.li.append(dim_ar)
        init_pos = []
        dim_ar = []


def sin_dim_tble(init_pos,dim_list,path_len):



